#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
/*    char *word = "My Dyn Lib";
    int len = _strlen(word);
    printf("%d\n", len);*/
    return (EXIT_SUCCESS);
}
